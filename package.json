{
  "name": "band-members",
  "version": "1.0.0",
  "private": true,
  "description": "Rock Band Family Tree — static site with a Netlify Functions + Netlify Blobs backend for shared band submissions.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "@netlify/blobs": "^8.1.0"
  }
}
