node_modules/
package-lock.json

# Ingestion pipeline outputs — regenerated by scripts/ingest-musicbrainz.mjs.
# The cache holds raw MB/Wikipedia responses; deleting it forces fresh fetches.
# The output directory holds proposed-candidate CSVs; nothing here belongs on main.
.cache/
scripts/output/
